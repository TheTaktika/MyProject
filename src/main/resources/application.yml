spring:
  datasource:
    url: jdbc:postgresql://localhost/my_project
    username: postgres
    password: qwerty
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true